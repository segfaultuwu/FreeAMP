<UserControl x:Class="FreeAMP.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:res="clr-namespace:FreeAMP.Resources"
             mc:Ignorable="d">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="72"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->

        <Grid Grid.Row="0"
              Margin="8">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="{x:Static res:Strings.MusicLibrary}"
                       VerticalAlignment="Center"
                       FontSize="26"
                       FontWeight="SemiBold"
                       Foreground="#F5F5FA"/>

            <TextBox x:Name="SearchTextBox"
                     Grid.Column="1"
                     Height="40"
                     VerticalAlignment="Center"/>
        </Grid>

        <!-- TRACKS -->

        <ListView Grid.Row="1"
                  x:Name="TracksListView"
                  Margin="8"
                  SelectionChanged="TracksListView_SelectionChanged">

            <ListView.View>
                <GridView>

                    <GridViewColumn Header="#"
                                    Width="50"
                                    DisplayMemberBinding="{Binding Number}"/>

                    <GridViewColumn Header="{x:Static res:Strings.TitleColumn}"
                                    Width="360"
                                    DisplayMemberBinding="{Binding Title}"/>

                    <GridViewColumn Header="{x:Static res:Strings.ArtistColumn}"
                                    Width="220"
                                    DisplayMemberBinding="{Binding Artist}"/>

                    <GridViewColumn Header="{x:Static res:Strings.AlbumColumn}"
                                    Width="220"
                                    DisplayMemberBinding="{Binding Album}"/>

                    <GridViewColumn Header="{x:Static res:Strings.DurationColumn}"
                                    Width="100"
                                    DisplayMemberBinding="{Binding Duration}"/>

                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>